---
- name: Provision runc-fork security-scan self-hosted runner
  hosts: runners
  become: true
  gather_facts: true

  vars:
    go_version: "1.24.2"
    gha_runner_version: "2.319.1"

  roles:
    - role: base
    - role: scanner_host
    - role: gha_runner
